<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_youtube_api"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context="template.cheng.hollis.template.YoutubeAPIActivity">

    <include layout="@layout/toolbar" />

    <LinearLayout
        android:id="@+id/ownerArea_framelayout"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:background="@color/Black"
        android:orientation="vertical"
        android:weightSum="5">

        <android.support.v4.view.ViewPager
            android:id="@+id/ownerArea_viewpager"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_margin="3dp"
            android:layout_weight="4" />

        <me.relex.circleindicator.CircleIndicator
            android:id="@+id/circleIndicator"
            android:layout_width="match_parent"
            android:layout_height="20dp"
            android:layout_weight="1"
            android:background="@color/Black"
            app:ci_animator="@anim/indicator_no_animator"
            app:ci_drawable="@drawable/icon_active_bookmark"
            app:ci_drawable_unselected="@drawable/icon_inactive_bookmark" />

    </LinearLayout>

</LinearLayout>
